---
name: tapd
description: WorkBridge 的 TAPD 兼容入口；用于明确的 TAPD 需求、任务和研发协作请求。
---

# TAPD 兼容入口

读取同仓库 `../workbridge/SKILL.md`，默认选择 TAPD 连接，然后进入同一套 WorkBridge 流程。安装时必须同时安装 workbridge；不要复制或保留第二套业务规则。用户明确选择其他平台时遵循新输入。
