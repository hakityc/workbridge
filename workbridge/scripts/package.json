{
  "name": "workbridge",
  "version": "0.1.0-rc.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "bin": { "workbridge": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@inquirer/prompts": "7.10.1",
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": { "@types/node": "22.15.3", "typescript": "5.9.3" }
}
